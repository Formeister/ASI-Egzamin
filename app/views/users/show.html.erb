<% provide(:title, @user.name) %>
<div class="row">
  <div class="col-md-offset-4 col-md-4">
    <section class="user_info text-center" style="padding-top: 20px">
      <h2>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h2>
    </section>
  </div>
</div>